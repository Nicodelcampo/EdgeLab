# Resolución de owner Kaggle — dataset NQ pre-holdout

**Decisión:** `ACCEPT_NICOLASBUTTARO_AS_CANONICAL_KAGGLE_CUSTODIAN`  
**Emitida:** 2026-08-28T21:32:24.058Z  
**Autoridad:** propietario del proyecto  
**Estado:** `OWNER_IDENTITY_RECONCILED`

## Dataset canónico

```text
OWNER      = nicolasbuttaro
SLUG       = edgelab-ticks-nq-preholdout
DATASET_ID = nicolasbuttaro/edgelab-ticks-nq-preholdout
VERSION    = v1
VISIBILITY = private
```

La decisión acepta `nicolasbuttaro` como cuenta Kaggle custodio autorizada para el paquete NQ pre-holdout. No se afirma que los nombres de cuenta sean identidades legales equivalentes; se registra una delegación/custodia explícitamente aceptada por el propietario del proyecto.

## Tratamiento del metadata anterior

El metadata pre-upload con ID `nicodelcampo/edgelab-ticks-nq-preholdout` y hash `2114192...` se preserva como evidencia histórica de la ceremonia original. No debe reutilizarse para una futura versión o recreación.

Cualquier metadata futuro debe usar:

```text
nicolasbuttaro/edgelab-ticks-nq-preholdout
```

Su hash deberá calcularse después de generarlo; no se inventa ni proyecta.

## Alcance

Esta resolución corrige únicamente el gate de owner. No modifica bytes, hashes, inventario, frontera temporal ni versión del dataset y no autoriza kernels.

```text
OWNER_IDENTITY_RECONCILED = true
POST_UPLOAD_REHASH        = pending
SCIENTIFIC_RUN_AUTHORIZED = false
```

## Aporte al referente

`nicolasbuttaro` queda fijado como custodio Kaggle canónico por decisión expresa, preservando el metadata original como evidencia histórica y manteniendo el rehash y toda ejecución científica cerrados.
