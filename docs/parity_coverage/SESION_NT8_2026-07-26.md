# Sesión de NT8 — lista final consolidada

**Fecha**: 2026-07-26 · Reemplaza la lista preliminar de
`ESTADO_Y_REEXPORT_2026-07-26.md` §4. **Cuatro exports.**
**`Gaps2` está anclado y no se toca.**

---

## Paso 0 — instalar los `.cs` corregidos

Reemplazo **in place** en `%USERPROFILE%\Documents\NinjaTrader 8\bin\Custom\Indicators\`.
**Nunca** copias dentro de `bin\Custom` (incidente 2026-07-25: dos definiciones de
la misma clase ⇒ CS0111/CS0102/CS0121/CS0229). Respaldos a `archive/nt8_cs_backup/`,
**fuera** de `bin\Custom`.

| archivo | versión | sha256 canónico (sin región generada) |
|---|---|---|
| `nt8/HFTZones2.cs` | **v2.3** | `9bdbcc8108d8dc3248bf0b23b18e2bbf53765a8a7fdfbb86ebf9f0e35f04fd32` |
| `nt8/AACloseOpenDiffs.cs` | **v1.2** | `e4f5f17b7a2f29fe85299575a4c4ab45b88b29414cb3ef7547d9616775ed2557` |

Los otros dos no cambiaron — no hace falta reinstalarlos:

| archivo | versión | sha256 |
|---|---|---|
| `nt8/VolTicksPOC2.cs` | v2.1 | `48e0718a055958f0b2a325cdee53517e449989c6b43ecfe50e7b4d634278845d` |
| `nt8/aVolCellPOI2.cs` | v2.0 | `4ad4c671333c0b5c214d3d2c3d4c75a6a7dd4f616ee26bc8aaa7d31bb0ead6ed` |

Verificación antes de entregarlos a NT8:

```bash
.venv/Scripts/python tools/check_nt8_cs.py --ulp nt8/HFTZones2.cs nt8/AACloseOpenDiffs.cs
```

Debe dar `[OK]` en los dos, `meta=…=2.3` y `…=1.2`, y `SIN TRIAJE: 0`.

> **`AACloseOpenDiffs` subió a v1.2, no v1.1.** v1.1 era la corrección de enteros;
> v1.2 agrega la columna `ind_version` a **cada fila** del logger de research
> (Decisión B). Va por fila y no en un header porque ese archivo **mergea**
> corridas distintas: una versión a nivel de archivo sería una afirmación falsa
> sobre el contenido mezclado. Sin ese campo el logger no se puede filtrar por
> versión — que es exactamente el hueco que dejó pasar el defecto de v1.0 durante
> todo su histórico.

---

## Los cuatro exports

### Export 1 — HFTZones2 v2.3

| campo | valor |
|---|---|
| indicador | **HFTZones2** v2.3 |
| instrumento | **6E 09-26** |
| resolución | **1 Minute** |
| rango de carga | **2026-07-06 → 2026-07-17** |
| params | defaults (`AdaptiveMode=true`, `PenetrationTicks=1`) |
| `EventLogPath` | `E:\EdgeLab\oracles\HFTZones2_adaptive_6E_0926_v23.csv` |

**Por qué**: el `.cs` estaba en v2.2 con `inside` comparando `double` mientras el
kernel Python ya era v2.3 en enteros — desalineados por construcción. El oráculo
`_v22` no sirve para validar v2.3.

---

### Export 2 — AACloseOpenDiffs v1.2

| campo | valor |
|---|---|
| indicador | **AACloseOpenDiffs** v1.2 |
| instrumento | **6E 09-26** |
| resolución | **25 Tick** *(cualquiera sirve — que pruebe de paso la independencia del `bar_spec`)* |
| rango de carga | **2026-07-09 → 2026-07-17** |
| params | `MinDiffTicks=1`, `ExtendBars=50`, **`FiltrarPorPercentil=false`** |
| `Ruta CSV de paridad` | `E:\EdgeLab\oracles\AACloseOpenDiffs_6E_0926_v12.csv` |

**Control de sanidad**: se esperan **notablemente más zonas** que el oráculo
anterior (1595 apareadas). Si trae la misma cantidad, el `.cs` instalado no es el
v1.2 — conviene mirarlo antes de seguir con los otros exports.

> **El logger de research arranca de cero.** Su archivo anterior está en
> cuarentena (ver §Decisión B), así que el `.cs` va a crear uno nuevo en
> `D:\A  Trading\loggers\AACloseOpenDiffs.csv` — limpio y ya con la columna
> `ind_version`. **Ese archivo es el inicio del histórico limpio.**

---

### Export 3 — aVolCellPOI2, rango largo

| campo | valor |
|---|---|
| indicador | **aVolCellPOI2** v2.0 *(sin cambios)* |
| instrumento | **6E 09-26** |
| resolución | **1 Minute** |
| rango de carga | **2026-06-12 → 2026-07-17** ⟵ el cambio importante |
| params | defaults (`lookback_sessions=20`, `percentile=99.5`, `export_floor=95`) |
| `EventLogPath` | `E:\EdgeLab\oracles\aVolCellPOI2_6E_0926_denso.csv` |

**Por qué el rango largo**: con `lookback_sessions=20`, el export actual llega a
`session_count=18` y **nunca llena la ventana**; por la regla de ventana llena la
región comparable queda **vacía**. No es un FAIL de kernel: es un oráculo que no
alcanza para afirmar ni negar paridad. Cargando desde 2026-06-12 hay ~20 sesiones
de warmup antes del 2026-07-13.

> Son ~25 días de M1. Si NT8 tarda mucho o recorta la carga, avisá y ajustamos —
> pero con menos de 20 sesiones de warmup este export no aporta nada nuevo.

---

### Export 4 — VolTicksPOC2 con ventana llena *(respuesta a tu pregunta)*

**Sí, conviene aprovechar la sesión.** No porque falle —ya da PASS— sino porque
recupera evidencia:

| | actual | con este export |
|---|---:|---:|
| pares comparados | 37 | **44** |
| zonas excluidas por ventana incompleta (py / nt8) | 14 / 7 | **0 / 0** |

Las 4 zonas NT8 con ventana incompleta son todas del 2026-07-09 temprano
(`window_count` 1569, 1571, 1576, 1640). El export actual arrancó con sólo 604
barras de warmup. Con `ratio_window_bars=2000` sobre M1 hacen falta ~2000 barras
antes de la primera zona ≈ **33 h de trading ≈ 2 sesiones + margen**.

| campo | valor |
|---|---|
| indicador | **VolTicksPOC2** v2.1 *(sin cambios)* |
| instrumento | **6E 09-26** |
| resolución | **1 Minute** |
| rango de carga | **2026-07-02 → 2026-07-17** ⟵ 3+ sesiones de warmup |
| params | defaults (`avg_period=200`, `ratio_window_bars=2000`, `detection_percentile=99.5`, `min_ratio_samples=500`, `export_floor=95`) |
| `EventLogPath` | `E:\EdgeLab\oracles\VolTicksPOC2_6E_0926_warmup.csv` |

**Lo que compra**: hoy el PASS es sobre una región que *elegimos* (la de ventana
llena). Con warmup el PASS es sobre la región **completa**, y las 7 discrepancias
que hoy se excluyen por construcción se convierten en 7 acuerdos verificados — o
revelan un diff real, que sería un hallazgo. Es la diferencia entre "pasa donde
miramos" y "pasa".

**Si la sesión se hace larga, éste es el que se puede saltear**: es el único de
los cuatro cuyo kernel ya está en PASS.

---

## Resumen de la sesión

| # | indicador | versión | resolución | rango de carga | archivo nuevo |
|---|---|---|---|---|---|
| 1 | HFTZones2 | v2.3 | 1 Minute | 2026-07-06 → 07-17 | `HFTZones2_adaptive_6E_0926_v23.csv` |
| 2 | AACloseOpenDiffs | v1.2 | 25 Tick | 2026-07-09 → 07-17 | `AACloseOpenDiffs_6E_0926_v12.csv` |
| 3 | aVolCellPOI2 | v2.0 | 1 Minute | 2026-06-12 → 07-17 | `aVolCellPOI2_6E_0926_denso.csv` |
| 4 | VolTicksPOC2 | v2.1 | 1 Minute | 2026-07-02 → 07-17 | `VolTicksPOC2_6E_0926_warmup.csv` |

Todos en `E:\EdgeLab\oracles\`. Timezone del chart: la tuya
(`America/Argentina/Buenos_Aires`) — **verificada** contra los 7 eventos de sesión
del oráculo de HFTZones2: 19:00 local = 17:00 CT = 22:00 UTC.

**No se re-exporta**: `Gaps2` (anclado, 1316/1316) ni `BigTrap2` `time:1` (PASS,
el `.cs` no cambió).

---

## Un pendiente que necesita tu mano antes de empezar

`oracles\AACloseOpenDiffs_6E_0926.csv` (el oráculo v1.0) **no se pudo mover a
cuarentena**: está abierto por otro proceso, casi seguro NT8. La copia forense ya
está en `archive/cuarentena/aacloseopendiffs_pre_v1.1/`, y el filtro estructural
**ya lo bloquea** aunque siga en su lugar — así que no es urgente. Cuando cierres
NT8:

```bash
rm "E:/EdgeLab/oracles/AACloseOpenDiffs_6E_0926.csv"
```

*(la copia en cuarentena conserva el original byte a byte)*
